#include "main.h"

/**
 * main - Entry point for the program that calls times_table function.
 *
 * Return: Always 0 (Success).
 */
int main(void)
{
    times_table(); /* Call the function to print the times table */
    return (0);
}
